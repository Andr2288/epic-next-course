/**
 * summary controller
 */

import { factories } from '@strapi/strapi';

// Regenerate Strapi types after adding this API: `npm run strapi develop` once
export default factories.createCoreController('api::summary.summary' as never);
